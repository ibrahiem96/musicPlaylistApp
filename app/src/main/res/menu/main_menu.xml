<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/createplaylist_menu"
        android:title="@string/make_playlist"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/invert_menu"
        android:title="@string/invert_selection"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/clearall_menu"
        android:title="@string/clear_all"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/checkall_menu"
        android:title="@string/check_all"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/refresh"
        android:title="Refresh"
        app:showAsAction="ifRoom"/>
</menu>